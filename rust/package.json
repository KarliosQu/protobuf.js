{
  "name": "protobuf-rust-core",
  "version": "0.1.0",
  "description": "Rust core for protobuf.js",
  "main": "index.js",
  "napi": {
    "name": "protobuf-core",
    "triples": {
      "defaults": true
    }
  },
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0"
  },
  "engines": {
    "node": ">= 10"
  }
}
